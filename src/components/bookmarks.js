import React, { Component, Fragment } from 'react'


class Bookmarks extends Component {

    componentDidMount() {
        //todo fetch bookmarks of userID passed
    }

    render() {

        return (
            <Fragment>
            //todo map over bookmarks to create a list of news and video Items
            </Fragment>
        )
    }
}

export default connect(Bookmarks)